# Kova research crawler

Периодичен анализ на извадка от български фирмени сайтове. Захранва три неща:
доклада за техническата готовност (вкл. за AI търсачки), списъка за контакт и
каталога с автоматизации.

**Това не е анализаторът на kova.bg.** Анализаторът обслужва един човек в реално
време през Cloudflare Worker; този инструмент обработва списък офлайн и
произвежда таблица за статистика. Различен модел на изпълнение, затова е отделен
Node пакет — не се деплойва в Cloudflare.

## Модел на изпълнение

Три отделни команди (collect ≠ derive, т. 2 от спецификацията):

```bash
# 1. Събиране — тегли и записва суровото. Нищо не измерва.
npm run crawl  -w @kova/crawler -- --targets targets.csv --run-id 2026-q3

# 2. Извличане — чисти функции над артефактите. Нула мрежа.
npm run derive -w @kova/crawler -- --run-id 2026-q3

# 3. Износ — широка CSV таблица, ред на домейн.
npm run export -w @kova/crawler -- --run-id 2026-q3
```

Смисълът на разделянето: измислиш нова метрика през ноември, пускаш `derive`
върху артефактите от август и имаш стойността ѝ за всички минали периоди.

Пуска се ръчно на тримесечие (`workflow_dispatch`, без планировчик).

## Съхранение

- **SQLite** (`crawler.db`, вграденият `node:sqlite`) — схема `runs`, `targets`,
  `pages`, `fetches`, `metrics`, `outreach`. `targets` живее отделно от `runs`,
  за да е стабилна извадката между тримесечията. Ред с `domain=NULL` е валиден —
  „фирми без сайт" е знаменателят на доклада.
- **Артефакти** — gzip на диск: `artifacts/{run_id}/{domain}/{page_slug}/{kind}.gz`.
  Не в базата. И двете папки, и `*.db`, са в `.gitignore`.

### Трайно съхранение (Cloudflare R2)

Локално базата и артефактите просто остават на диска между пусканията.
В GitHub Actions рънърът е ефимерен, затова workflow-ът пази състоянието в
**Cloudflare R2**: възстановява `crawler.db` (и артефактите на текущия
`run_id`) в началото и качва обратно базата, артефактите и CSV-то накрая.
Така базата натрупва между тримесечията, а старите артефакти остават за
повторен `derive` с нова метрика.

Конфигурацията идва изцяло от GitHub secrets/variables (нищо чувствително в
кода) и живее в environment **`production`** — workflow-ът декларира
`environment: production`, иначе стойностите не се виждат от job-а.
Добави ги в **Settings → Environments → production**:

| Вид | Име | Стойност |
|---|---|---|
| Variable | `R2_BUCKET` | име на R2 bucket-а |
| Variable или Secret | `R2_ACCOUNT_ID` | Cloudflare account ID |
| Variable или Secret | `R2_ACCESS_KEY_ID` | от R2 API token |
| **Само Secret** | `R2_SECRET_ACCESS_KEY` | от R2 API token |

Тайният ключ задължително е Secret — Variables не се маскират в логовете.
Ако environment-ът има protection rules (например задължително одобрение),
пускането ще изчака одобрение, преди да тръгне.

R2 API token се създава от Cloudflare → R2 → *Manage API Tokens* (права за
четене и запис върху bucket-а). Пускай по едно събиране наведнъж — базата е
един обект в R2 и паралелни пускания биха се презаписали.

Структурата в bucket-а: `crawler.db`, `artifacts/{run_id}/…`, `exports/{run_id}.csv`.

## Статус на всяка метрика (т. 8)

`passed | failed | not_applicable | not_measurable | error` — никога слети в
булево. Сайт без форми не се проваля на `contact_form_field_count`, а е
`not_applicable`; сайт, върнал грешка при collect, е `error`, не `false`.

## Обхват

- **Многостранично обхождане** (т. 3): начална + по една страница за роля
  (контакти / за нас / услуги / цени / кариери) + до няколко „други", таван 10.
  Навигационните връзки имат приоритет пред sitemap.
- **Артефакти** (т. 4): начална ×3 UA (raw), рендиране в браузър, headers,
  robots/llms/sitemap, DNS (MX), TLS, RDAP (възраст на домейна). Подстраниците
  се теглят с браузърен UA.
- **PageSpeed Insights** (mobile): само ако е зададен `PAGESPEED_API_KEY`
  (secret в workflow-а); иначе метриките са `not_measurable`.
- **~50 derive метрики**: технически SEO, AI четимост (JS зависимост, cloaking
  по UA, robots за AI ботове, schema), стек и възраст (cms/framework/is_spa,
  domain_age, mx, tls), достъпност, свежест (sitemap lastmod), производителност
  и оперативните сигнали за писмата (форми, документи, кариери, екип, търговия).

Изнесените чисти функции живеят в `packages/shared-audit`. По-тежките проверки
от `src/lib/analyzer.ts` (анализаторът на сайта) се консолидират там на следващ
етап. Все още предстои: пълните performance полета без PSI ключ, WHOIS държава
на регистрация, и шаблонният fallback за роля, когато навигацията не даде
страницата (сега: навигация + sitemap).

## Етика

- Идентифицира се честно: `KovaResearchBot/1.0 (+https://kova.bg/research)`.
  Направи страницата на този адрес — кой си, какво изследваш, как да те изключат.
- robots.txt забраната **не се заобикаля** — записва се като стойност.
- Третият UA вариант декларира AI crawler, за да мери cloaking (различно ли е
  съдържанието според това кой пита). Съзнателна изследователска техника.

## Изисквания

Node 22+ (за `node:sqlite` и `--experimental-strip-types`) и Chromium за
Playwright. В тази среда Chromium е на `/opt/pw-browsers/chromium`; иначе
задай `CHROMIUM_PATH`.
