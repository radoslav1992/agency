# Календарът за запазване на разговор

Посетителят избира час на `/booking/`, гласовият агент може да запази час по
телефона, а ти управляваш всичко от `/admin/`. Всичко живее на Cloudflare — D1
за данните, Worker за логиката. Няма външна услуга и няма месечна такса.

## Какво къде стои

| Файл | За какво отговаря |
| --- | --- |
| `src/data/booking.mjs` | Работно време, продължителност, буфер, празници. **Единственото място, което се редактира при промяна на часовете.** |
| `src/lib/time.ts` | Местно време ⇄ UTC. Без библиотека — `Intl` в workerd носи пълните данни за зоните. |
| `src/lib/booking.ts` | Свободните часове и запазването. Един двигател за сайта, агента и панела. |
| `src/lib/ics.ts` | Файлът за календара на посетителя. |
| `src/lib/notify.ts` | Известието до студиото. |
| `migrations/0001_booking.sql` | Схемата. |

## Пускане

### 1. Базата

```sh
npx wrangler d1 create kova-bookings
```

Командата връща `database_id`. Сложи го в `wrangler.jsonc` на мястото на
`ЗАМЕНИ_МЕ`, после приложи схемата — веднъж локално, веднъж на живо:

```sh
npx wrangler d1 execute kova-bookings --local  --file=migrations/0001_booking.sql
npx wrangler d1 execute kova-bookings --remote --file=migrations/0001_booking.sql
```

### 2. Секретите

```sh
npx wrangler secret put ADMIN_PASSWORD   # входът в /admin/
npx wrangler secret put AGENT_TOKEN      # с него ElevenLabs се представя
```

За `AGENT_TOKEN` става всеки дълъг случаен низ:

```sh
openssl rand -hex 32
```

И двата се проверяват **затворено**: липсва ли `ADMIN_PASSWORD`, панелът
връща 503 и не се отваря на никого; липсва ли `AGENT_TOKEN`, инструментите на
агента не приемат нищо. Забравена настройка не бива да значи отворена врата.

За местна разработка същите две отиват в `.dev.vars` (файлът е в `.gitignore`).

### 3. Деплой

```sh
npm run deploy
```

## Инструментите за ElevenLabs

В таблото на агента се добавят два инструмента от вид **webhook**. И на двата
се слага заглавие `Authorization` със стойност `Bearer <AGENT_TOKEN>`.

### `check_availability` — GET `https://kova.bg/api/agent/slots`

Без параметри в тялото. Връща най-много три дни по четири часа плюс готово
изречение в полето `speak`.

> Опиши го на агента като: „Проверява кога Радослав е свободен. Викай го,
> преди да предложиш час. Никога не измисляй часове.“

```json
{
  "timezone": "Europe/Sofia",
  "duration_minutes": 30,
  "days": [
    {
      "date": "2026-08-18",
      "label": "вторник, 18 август",
      "slots": [{ "start_utc": 1787050800000, "time": "14:00" }]
    }
  ],
  "speak": "Най-близките свободни часове са във вторник, 18 август: 14:00, 14:40…"
}
```

### `book_call` — POST `https://kova.bg/api/agent/book`

```json
{
  "start_utc": 1787050800000,
  "name": "Мария Иванова",
  "phone": "+359...",
  "email": "по избор",
  "note": "по избор",
  "locale": "bg"
}
```

> Опиши го така: „`start_utc` е точното число, което си получил от
> `check_availability`. Не го пресмятай сам. Имейл искай само ако човекът
> сам го предложи — продиктуван на глас, той почти винаги е грешен.“

Отговорът винаги носи `speak` — изречение, което агентът може да каже
дословно. При зает час идва и списък с най-близките свободни, за да не се
налага втори инструмент насред разговора.

## Как се променя работното време

Всичко е в `src/data/booking.mjs`:

```js
export const HOURS = {
  1: [['10:00', '13:00'], ['14:00', '18:00']], // понеделник
  // липсващ ден = почивен
};
```

Слотът трябва да **свърши** вътре в интервала, затова `10:00–13:00` с
30-минутен разговор и 10 минути буфер дава начала в 10:00, 10:40, 11:20 и
12:00 — но не и 12:40, защото 13:10 е извън интервала.

`DURATION_MINUTES` е 30, защото точно това пише на страницата с процеса
(„Първоначален разговор: 30 минути, безплатно“). Смениш ли го, смени и там.

## Какво съзнателно липсва

**Имейл до посетителя.** Връзката `send_email` на Cloudflare доставя само до
адрес, потвърден като получател в акаунта — тоест само до теб. Посетителят
получава екран с потвърждение и файл за календара (в който има напомняне 30
минути преди часа и връзка за отказ), но не и писмо. Ако това стане важно,
шевът е `notifyVisitor` в `src/lib/notify.ts`; днес функцията се вика, но е
празна.

**Връзка с личния ти календар.** Системата знае само за собствените си
запазвания. Ако си на зъболекар във вторник следобед, тя ще предложи вторник
следобед — освен ако не затвориш времето от панела. Истинската поправка е
четене на заетостта от Google Calendar; дотогава панелът е ръчният вариант.

**Напомняне преди разговора.** Няма изпращане на съобщение ден по-рано.
Файлът за календара носи собствено напомняне, но само за тези, които са го
свалили.

## Поддръжка

```sh
# кой е записан
npx wrangler d1 execute kova-bookings --remote \
  --command "SELECT start_utc, name, phone, source FROM bookings WHERE status='confirmed' ORDER BY start_utc"

# да се разчистят стари записи (по желание — те не пречат)
npx wrangler d1 execute kova-bookings --remote \
  --command "DELETE FROM bookings WHERE start_utc < unixepoch('now','-1 year')*1000"
```

Празниците в `HOLIDAYS` са ръчен списък по години. Изтеклите редове не пречат,
но новата година трябва да се добави.
