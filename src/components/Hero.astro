---
import { Image } from 'astro:assets';
import heroPortrait from '../assets/hero-portrait.png';
import { SITE } from '../data/site.mjs';
---

<section class="hero container">
  <div class="hero__copy">
    {
      SITE.availability && (
        <p class="availability">
          <span class="availability__dot" aria-hidden="true" />
          {SITE.availability}
        </p>
      )
    }

    <h1 class="hero__title">
      Автоматизирам работата, която екипът ти още <span class="accent">върши на ръка</span>.
    </h1>

    <p class="hero__lead">
      Асистент, който отговаря по договорите и наръчниците ви. Система вместо седемте таблици в
      Excel. Обработка на заявки, която върви, без някой да я бута всеки ден.
    </p>

    <div class="hero__actions">
      <a class="btn btn--primary" href="/contact/">Запази безплатен разговор</a>
      <a class="btn btn--outline" href="/#services">Виж как работя</a>
    </div>
    <p class="hero__subnote">
      30 минути, без ангажимент. Ако процесът ти не става за автоматизация, ще ти го кажа в
      разговора.
    </p>

    <p class="hero__analyzer">
      Имаш сайт? <a href="/analyzer/">Провери го безплатно за 15 секунди →</a>
    </p>

    <ul class="hero__facts">
      <li>🤝 Работиш с мен, не с акаунт мениджър</li>
      <li>🧪 Тръгваме с малък пилот, преди да влагаш бюджет</li>
      <li>🎯 Целта, срокът и цената са ясни от офертата</li>
    </ul>
  </div>

  <div class="hero__media">
    <div class="hero__blob" aria-hidden="true"></div>
    <Image
      class="hero__image"
      src={heroPortrait}
      alt={SITE.owner.name}
      widths={[420, 640, 900]}
      sizes="(max-width: 900px) 90vw, 440px"
      loading="eager"
      fetchpriority="high"
    />
    <div class="hero__badge">
      <p class="hero__badge-name">{SITE.owner.name}</p>
      <p class="hero__badge-role">{SITE.owner.role}</p>
    </div>
  </div>
</section>

<style>
  .hero {
    padding-block: 84px 64px;
    display: grid;
    grid-template-columns: 1.15fr 0.85fr;
    gap: 56px;
    align-items: center;
  }

  .availability {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    margin: 0;
    background: var(--panel);
    border: 1px solid var(--line);
    padding: 7px 14px;
    border-radius: 999px;
    font-size: 13px;
    font-weight: 500;
    color: var(--ink-muted);
  }

  .availability__dot {
    width: 7px;
    height: 7px;
    border-radius: 50%;
    background: var(--success);
    flex: none;
  }

  .hero__title {
    font-size: clamp(36px, 6vw, 58px);
    line-height: 1.04;
    letter-spacing: -0.035em;
    margin: 22px 0 0;
  }

  .accent {
    color: var(--accent);
  }

  .hero__lead {
    font-size: 19px;
    line-height: 1.55;
    color: var(--ink-body);
    max-width: 520px;
    margin: 24px 0 0;
  }

  .hero__actions {
    display: flex;
    flex-wrap: wrap;
    gap: 12px;
    margin-top: 32px;
  }

  .hero__subnote {
    font-size: 13px;
    line-height: 1.5;
    color: var(--ink-subtle);
    margin: 12px 0 0;
    max-width: 460px;
  }

  .hero__analyzer {
    font-size: 14.5px;
    margin: 22px 0 0;
    color: var(--ink-body);
  }

  .hero__analyzer a {
    font-weight: 600;
    color: var(--accent);
  }

  .hero__facts {
    display: flex;
    flex-wrap: wrap;
    gap: 12px 28px;
    margin: 32px 0 0;
    padding: 0;
    list-style: none;
    font-size: 14px;
    color: var(--ink-muted);
  }

  .hero__media {
    position: relative;
  }

  .hero__blob {
    position: absolute;
    inset: 12% -4% -2% 6%;
    background: var(--bg-blush);
    border-radius: 32px;
  }

  .hero__image {
    position: relative;
    width: 100%;
    height: auto;
    display: block;
    filter: var(--shadow-hero);
  }

  .hero__badge {
    position: absolute;
    left: -8px;
    bottom: 18px;
    background: var(--panel);
    border: 1px solid var(--line);
    border-radius: 16px;
    padding: 12px 16px;
    box-shadow: var(--shadow-card);
  }

  .hero__badge-name {
    font-weight: 700;
    font-size: 14px;
    margin: 0;
  }

  .hero__badge-role {
    font-size: 12.5px;
    color: var(--ink-muted);
    margin: 2px 0 0;
  }

  @media (max-width: 900px) {
    .hero {
      grid-template-columns: 1fr;
      gap: 40px;
      padding-block: 48px 40px;
    }

    .hero__media {
      order: -1;
      max-width: 420px;
      margin-inline: auto;
      width: 100%;
    }

    .hero__badge {
      left: 0;
    }
  }
</style>
