/**
 * Отзиви.
 *
 * ⚠️ Текстовете по-долу са ПРИМЕРНИ (идват от дизайна). Замени ги с реалните
 * отзиви и проекти — това е единственото място, което трябва да се пипне.
 * Щом `placeholder` стане `false` (или махнеш полето), бележката „Примерни
 * текстове“ над секцията изчезва автоматично.
 */

export type Testimonial = {
  /** Самият отзив, без кавички — те се добавят от дизайна. */
  quote: string;
  /** Име на клиента. */
  author: string;
  /** Позиция и/или фирма. */
  role: string;
  /** Инициали за аватара. Ако липсват, се генерират от името. */
  initials?: string;
  /** По желание: проектът, за който се отнася отзивът. */
  project?: {
    name: string;
    /** Линк към живия сайт/приложение — прави името кликаемо. */
    url?: string;
    /** Кратък измерим резултат, напр. „12 часа седмично спестени“. */
    result?: string;
  };
};

export const PLACEHOLDER_TESTIMONIALS = true;

export const TESTIMONIALS: Testimonial[] = [
  {
    quote:
      'Обясни ми всичко на нормален език и за две седмици имах сайт с онлайн поръчки. Оборотът в събота се удвои.',
    author: 'Мария Димитрова',
    role: 'собственик, сладкарница „Захарно“',
    initials: 'МД',
  },
  {
    quote:
      'AI асистентът върху документите ни спести на екипа около 12 часа седмично. Най-важното — посочва източника на всеки отговор.',
    author: 'Иван Петров',
    role: 'оперативен директор, логистична фирма',
    initials: 'ИП',
  },
  {
    quote:
      'Работили сме с три агенции. Радо е първият, който ни каза кое НЕ ни трябва. Това ни спести бюджет.',
    author: 'Елена Стоянова',
    role: 'управител, счетоводна кантора',
    initials: 'ЕС',
  },
];

/** Инициали от име, когато не са зададени изрично. */
export function initialsOf(t: Testimonial): string {
  if (t.initials) return t.initials;
  return t.author
    .split(/\s+/)
    .slice(0, 2)
    .map((part) => part.charAt(0).toUpperCase())
    .join('');
}
