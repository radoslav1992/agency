/**
 * Кога може да се запази разговор.
 *
 * Единственото място, което описва работното време. Двигателят в
 * `src/lib/booking.ts` не знае нищо друго за календара освен този файл — така
 * смяната на часовете е редакция на данни, а не промяна на логика.
 *
 * `.mjs`, а не `.ts`, по същата причина като `site.mjs`: файлът се чете и от
 * конфигурацията при build, където TypeScript още не е минал.
 */

/** Зоната, в която са изписани часовете отдолу. Всичко останало е UTC. */
export const TIMEZONE = 'Europe/Sofia';

/**
 * Продължителност на разговора в минути.
 *
 * 30 не е избрано тук — стои в „Първоначален разговор: 30 минути, безплатно“
 * на страницата с процеса. Ако се смени, да се смени и там; две различни
 * числа за едно и също нещо са по-лоши от неудобно число.
 */
export const DURATION_MINUTES = 30;

/**
 * Почивка след всеки разговор, в минути.
 *
 * Мрежата от слотове върви на `DURATION_MINUTES + BUFFER_MINUTES`, тоест на
 * 40 минути. Без буфер два поредни разговора се допират и всяко надхвърляне
 * с минута прави следващия закъснял.
 */
export const BUFFER_MINUTES = 10;

/**
 * Най-рано колко часа напред може да се запази.
 *
 * Пази те от разговор, насрочен за след пет минути, докато си в друга среща
 * и не си погледнал пощата.
 */
export const MIN_LEAD_HOURS = 12;

/** Колко дни напред се показват свободни часове. */
export const HORIZON_DAYS = 21;

/**
 * Работното време по дни от седмицата, в местно време.
 *
 * Ключът е денят по номерацията на `Date#getUTCDay` — 0 е неделя. Липсващ
 * ден значи почивен. Интервалите са включващи в началото и изключващи в
 * края: `['10:00', '13:00']` дава начала в 10:00, 10:40, 11:20, 12:00 и
 * 12:40, но не 13:00, защото разговорът трябва да свърши вътре в интервала.
 */
export const HOURS = {
  1: [['10:00', '13:00'], ['14:00', '18:00']], // понеделник
  2: [['10:00', '13:00'], ['14:00', '18:00']],
  3: [['10:00', '13:00'], ['14:00', '18:00']],
  4: [['10:00', '13:00'], ['14:00', '18:00']],
  5: [['10:00', '13:00'], ['14:00', '17:00']], // петък — по-кратък следобед
};

/**
 * Официални празници, в които не се работи. Формат `ГГГГ-ММ-ДД`.
 *
 * Ръчен списък нарочно: библиотека за българските празници тежи повече от
 * един ред на година, а Великден и без това се мести. Изтеклите редове може
 * да се трият, но не пречат.
 */
export const HOLIDAYS = [
  '2026-01-01', // Нова година
  '2026-03-03', // Освобождението
  '2026-04-10', // Велики петък
  '2026-04-13', // Великден
  '2026-05-01', // Ден на труда
  '2026-05-06', // Гергьовден
  '2026-05-24', // Св. св. Кирил и Методий
  '2026-09-06', // Съединението
  '2026-09-22', // Независимостта
  '2026-12-24',
  '2026-12-25',
  '2026-12-26',
];
