/**
 * Превключватели за цели части от сайта.
 *
 * Четат се при BUILD, а не при заявка, и това не е мързел — то е
 * единственото, което върши работа тук.
 *
 * Сайтът е предрендериран. Страницата на наръчниците, връзката в менюто,
 * sitemap-ът и `llms.txt` се изписват веднъж при построяване и се сервират
 * като статични файлове, без Worker-ът изобщо да се събужда. Променлива,
 * четена при заявка, няма как да махне връзка от менюто, което вече е
 * записано в четиридесет готови HTML файла — тя може да пази само пътищата,
 * които наистина минават през Worker-а.
 *
 * Затова превключвателят е променлива за СТРОЕЖА, не за изпълнението:
 *
 *   Cloudflare → Workers & Pages → agency → Settings → Build →
 *   променливи за строеж → `GUIDES_ENABLED = false`
 *
 * Смяната ѝ иска нов деплой (минута-две). В замяна изключеното наистина
 * изчезва: страница не се построява, връзка не се изписва, адресът връща
 * 404 вместо да стои скрит зад липсващо меню.
 *
 * По подразбиране е ВКЛЮЧЕНО. Липсваща променлива в нова среда трябва да
 * значи „както си беше“, а не тихо изчезнал магазин.
 */

/** `false`, `0`, `off`, `no` изключват. Всичко останало — включено. */
function enabled(value, fallback = true) {
  if (value === undefined || value === null || value === '') return fallback;
  return !/^(false|0|off|no)$/i.test(String(value).trim());
}

/*
 * Стойността влиза в пакета на Worker-а като ЛИТЕРАЛ, не се чете при заявка.
 *
 * `__GUIDES_ENABLED__` се заменя от Vite при строежа (виж `vite.define` в
 * `astro.config.mjs`). Първата версия четеше `process.env` и това мълчаливо
 * не работеше за маршрутите на Worker-а: там `process.env` се отваря при
 * ЗАЯВКА, вътре в workerd, където променливата от строежа я няма — тоест
 * страниците изчезваха, а `/api/guide-file` продължаваше да подава файла.
 *
 * В Node (когато `astro.config.mjs` внася този файл) заместване няма, затова
 * `typeof` пази обръщението и стойността идва от средата на строежа.
 */
const inlined = typeof __GUIDES_ENABLED__ !== 'undefined' ? __GUIDES_ENABLED__ : undefined;
const env = typeof process !== 'undefined' && process.env ? process.env : {};

export const FLAGS = {
  /**
   * Наръчниците: рафтът, страниците им, продажбата, свалянето и записването
   * за известие. Изключено — все едно ги няма.
   */
  guides: enabled(inlined ?? env.GUIDES_ENABLED),
};
